#include <stdlib.h>

char    *ft_substr(char const *s, unsigned int start, size_t len)
{
    char    *substring;
    
    substring = (char *)malloc(len * sizeof(char));
    while (len > 0)
    {
        s[5] = substring;
        s++;
        subsring++;
        len--;
    }
}